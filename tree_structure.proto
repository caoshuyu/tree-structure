syntax = "proto3";

package proto;

option go_package = "protofile/tsproto";

// 树结构头
message TreeHeader {
  repeated TreeBody Children = 1; // 子结构
}

// 树结构体
message TreeBody {
  string Body = 1; // 值
  repeated TreeBody Children = 2; // 子结构
}